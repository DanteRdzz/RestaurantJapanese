<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:RestaurantJapanese.Converters">

 <!-- ===== Converters ===== -->
    <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

    <!-- ===== Paleta Japonesa Ampliada ===== -->
    <Color x:Key="JpSakuraPink">#FFF8E9</Color>
    <Color x:Key="JpOffWhite">#FFFDF9</Color>
    <Color x:Key="JpCharcoal">#1E1E1E</Color>
    <Color x:Key="JpGray">#6E6E6E</Color>
    <Color x:Key="JpLightGray">#F5F5F5</Color>
    <Color x:Key="JpCrimson">#C0002A</Color>
    <Color x:Key="JpCrimsonDark">#990020</Color>
    <Color x:Key="JpInk">#101014</Color>
    <Color x:Key="JpLine">#FFE8DF</Color>
    
    <!-- Colores adicionales japoneses -->
    <Color x:Key="JpGold">#D4AF37</Color>
    <Color x:Key="JpBamboo">#8FBC8F</Color>
    <Color x:Key="JpWasabi">#7CB342</Color>
    <Color x:Key="JpPlum">#8E4585</Color>
    <Color x:Key="JpShadow">#33000000</Color>
    <Color x:Key="JpAccent">#FF6B6B</Color>

    <!-- Gradientes japoneses -->
    <LinearGradientBrush x:Key="JpGradientBg" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="{StaticResource JpSakuraPink}" Offset="0"/>
        <GradientStop Color="{StaticResource JpOffWhite}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="JpCardGradient" StartPoint="0,0" EndPoint="1,1">
 <GradientStop Color="{StaticResource JpOffWhite}" Offset="0"/>
     <GradientStop Color="{StaticResource JpLightGray}" Offset="1"/>
    </LinearGradientBrush>

    <!-- Brushes -->
    <SolidColorBrush x:Key="JpBgBrush" Color="{StaticResource JpSakuraPink}"/>
    <SolidColorBrush x:Key="JpSurfaceBrush" Color="{StaticResource JpOffWhite}"/>
    <SolidColorBrush x:Key="JpTextBrush" Color="{StaticResource JpInk}"/>
    <SolidColorBrush x:Key="JpMutedBrush" Color="{StaticResource JpGray}"/>
    <SolidColorBrush x:Key="JpCrimsonBrush" Color="{StaticResource JpCrimson}"/>
    <SolidColorBrush x:Key="JpCrimsonDarkBrush" Color="{StaticResource JpCrimsonDark}"/>
    <SolidColorBrush x:Key="JpLineBrush" Color="{StaticResource JpLine}"/>
    <SolidColorBrush x:Key="JpGoldBrush" Color="{StaticResource JpGold}"/>
    <SolidColorBrush x:Key="JpBambooBrush" Color="{StaticResource JpBamboo}"/>
  <SolidColorBrush x:Key="JpAccentBrush" Color="{StaticResource JpAccent}"/>
    <SolidColorBrush x:Key="JpShadowBrush" Color="{StaticResource JpShadow}"/>

    <!-- Radius -->
    <CornerRadius x:Key="JpRadiusXL">20</CornerRadius>
    <CornerRadius x:Key="JpRadiusL">16</CornerRadius>
    <CornerRadius x:Key="JpRadiusM">12</CornerRadius>
    <CornerRadius x:Key="JpRadiusS">8</CornerRadius>

<<<<<<< HEAD
    <!-- ===== Tipografía Mejorada ===== -->
    <!-- Título principal de página -->
    <Style x:Key="JpPageTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource JpTextBrush}"/>
        <Setter Property="FontSize" Value="36"/>
        <Setter Property="FontWeight" Value="Light"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Margin" Value="0,20,0,30"/>
    </Style>

    <!-- Subtítulo de página -->
    <Style x:Key="JpPageSubtitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource JpMutedBrush}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="LineHeight" Value="26"/>
        <Setter Property="Margin" Value="0,0,0,20"/>
    </Style>

=======
    <!-- ===== Tipograf�a Mejorada ===== -->
>>>>>>> adding return buttons
    <Style x:Key="JpTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource JpTextBrush}"/>
        <Setter Property="FontSize" Value="32"/>
   <Setter Property="FontWeight" Value="Light"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <Style x:Key="JpSubtitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource JpMutedBrush}"/>
        <Setter Property="FontSize" Value="16"/>
    <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="TextAlignment" Value="Center"/>
  <Setter Property="FontFamily" Value="Segoe UI"/>
    <Setter Property="LineHeight" Value="24"/>
 </Style>

    <Style x:Key="JpCardTitle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource JpTextBrush}"/>
        <Setter Property="FontSize" Value="20"/>
  <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <Style x:Key="JpCardDescription" TargetType="TextBlock">
     <Setter Property="Foreground" Value="{StaticResource JpMutedBrush}"/>
        <Setter Property="FontSize" Value="14"/>
  <Setter Property="FontWeight" Value="Normal"/>
 <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="LineHeight" Value="20"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <Style x:Key="JpErrorText" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource JpCrimsonBrush}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <!-- ===== Card Mejorada ===== -->
    <Style x:Key="JpCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource JpCardGradient}"/>
        <Setter Property="CornerRadius" Value="{StaticResource JpRadiusXL}"/>
 <Setter Property="Padding" Value="32,28"/>
     <Setter Property="BorderBrush" Value="{StaticResource JpLineBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Card especial para Admin Menu -->
    <Style x:Key="JpAdminCard" TargetType="Border" BasedOn="{StaticResource JpCard}">
        <Setter Property="MinHeight" Value="200"/>
    </Style>

    <!-- ===== Inputs Mejorados ===== -->
    <Style x:Key="JpTextBox" TargetType="TextBox">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="{StaticResource JpTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource JpLineBrush}"/>
     <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="{StaticResource JpRadiusM}"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Background" Value="{StaticResource JpSurfaceBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <Style x:Key="JpPasswordBox" TargetType="PasswordBox">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="{StaticResource JpTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource JpLineBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="{StaticResource JpRadiusM}"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Background" Value="{StaticResource JpSurfaceBrush}"/>
        <Setter Property="IsPasswordRevealButtonEnabled" Value="True"/>
     <Setter Property="PasswordRevealMode" Value="Peek"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FlowDirection" Value="LeftToRight"/>
    </Style>

    <!-- ===== Botones Mejorados ===== -->
    <Style x:Key="JpPrimaryButton" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource JpCrimsonBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="24,12"/>
        <Setter Property="CornerRadius" Value="{StaticResource JpRadiusM}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Template">
            <Setter.Value>
      <ControlTemplate TargetType="Button">
         <Grid>
        <Border x:Name="Root"
            Background="{TemplateBinding Background}"
             CornerRadius="{TemplateBinding CornerRadius}">
    <ContentPresenter
    HorizontalAlignment="Center"
    VerticalAlignment="Center"
            Content="{TemplateBinding Content}"/>
        </Border>

        <VisualStateManager.VisualStateGroups>
           <VisualStateGroup x:Name="CommonStates">
 <VisualState x:Name="Normal"/>
          <VisualState x:Name="PointerOver">
      <VisualState.Setters>
     <Setter Target="Root.Background" Value="{StaticResource JpCrimsonDarkBrush}"/>
    </VisualState.Setters>
                </VisualState>
            <VisualState x:Name="Pressed">
  <VisualState.Setters>
      <Setter Target="Root.Background" Value="{StaticResource JpCrimsonDarkBrush}"/>
           <Setter Target="Root.Opacity" Value="0.8"/>
                </VisualState.Setters>
       </VisualState>
     <VisualState x:Name="Disabled">
      <VisualState.Setters>
    <Setter Target="Root.Opacity" Value="0.4"/>
      </VisualState.Setters>
            </VisualState>
         </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
    </Grid>
     </ControlTemplate>
         </Setter.Value>
        </Setter>
    </Style>

<<<<<<< HEAD
    <!-- Botón Redondo Grande -->
    <Style x:Key="JpRoundButton" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource JpCrimsonBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="Height" Value="80"/>
        <Setter Property="CornerRadius" Value="40"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="40"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}">
                        </Border>
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"/>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="{StaticResource JpCrimsonDarkBrush}"/>
                                        <Setter Target="Root.Scale" Value="1.05"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="{StaticResource JpCrimsonDarkBrush}"/>
                                        <Setter Target="Root.Scale" Value="0.95"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Opacity" Value="0.4"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Botón Redondo Verde -->
    <Style x:Key="JpRoundButtonGreen" TargetType="Button" BasedOn="{StaticResource JpRoundButton}">
        <Setter Property="Background" Value="{StaticResource JpBambooBrush}"/>
    </Style>

    <!-- Botón Redondo Naranja -->
    <Style x:Key="JpRoundButtonOrange" TargetType="Button" BasedOn="{StaticResource JpRoundButton}">
        <Setter Property="Background" Value="{StaticResource JpAccentBrush}"/>
    </Style>

    <!-- Botón Secundario -->
=======
    <!-- Bot�n Secundario -->
>>>>>>> adding return buttons
    <Style x:Key="JpSecondaryButton" TargetType="Button" BasedOn="{StaticResource JpPrimaryButton}">
        <Setter Property="Background" Value="{StaticResource JpBambooBrush}"/>
    </Style>

    <!-- Bot�n Accent -->
    <Style x:Key="JpAccentButton" TargetType="Button" BasedOn="{StaticResource JpPrimaryButton}">
   <Setter Property="Background" Value="{StaticResource JpAccentBrush}"/>
    </Style>

    <!-- ===== ListView Mejorado ===== -->
    <Style x:Key="JpListView" TargetType="ListView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="CornerRadius" Value="{StaticResource JpRadiusM}"/>
     <Setter Property="Padding" Value="8"/>
    </Style>

    <!-- ===== ContentDialog ===== -->
    <Style TargetType="ContentDialog">
        <Setter Property="Foreground" Value="{StaticResource JpTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource JpSurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource JpRadiusL}"/>
    </Style>

    <!-- ===== Separador Japon�s ===== -->
<Style x:Key="JpSeparator" TargetType="Rectangle">
    <Setter Property="Height" Value="2"/>
        <Setter Property="Fill" Value="{StaticResource JpGoldBrush}"/>
        <Setter Property="Margin" Value="0,16"/>
     <Setter Property="RadiusX" Value="1"/>
        <Setter Property="RadiusY" Value="1"/>
    </Style>

    <!-- ===== Icono Decorativo ===== -->
    <Style x:Key="JpIcon" TargetType="TextBlock">
     <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="24"/>
      <Setter Property="Foreground" Value="{StaticResource JpGoldBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
     <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>