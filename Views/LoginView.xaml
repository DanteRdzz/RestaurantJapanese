<UserControl
    x:Class="RestaurantJapanese.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="360">
            <StackPanel Spacing="12">
                <TextBlock Text="Restaurant JP" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center"/>

                <TextBox PlaceholderText="Usuario"
                 Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- NÓTESE: le ponemos un x:Name para leer su Password -->
                <PasswordBox x:Name="Pwd"
                     PlaceholderText="Contraseña"
                     PasswordRevealMode="Peek"
                     IsPasswordRevealButtonEnabled="True"/>

                <!-- Aquí pasamos la contraseña como CommandParameter -->
                <Button Content="Iniciar sesión"
                Command="{Binding SignInCommand}"
                CommandParameter="{Binding ElementName=Pwd, Path=Password}"/>

                <TextBlock Text="{Binding Error}" Foreground="IndianRed" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>