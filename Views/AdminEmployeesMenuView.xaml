<UserControl
    x:Class="RestaurantJapanese.Views.AdminEmployeesMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource JpGradientBg}" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12">
                <TextBlock Text="👥" FontSize="28" VerticalAlignment="Center"/>
                <TextBlock Text="Gestión de Empleados" Style="{StaticResource JpTitle}"/>
                <TextBlock Text="👥" FontSize="28" VerticalAlignment="Center"/>
            </StackPanel>
            <Rectangle Style="{StaticResource JpSeparator}" Width="420" HorizontalAlignment="Center"/>
            
            <!-- Navigation tabs -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                <RadioButton Content="Listar" IsChecked="{Binding IsListSelected, Mode=TwoWay}" GroupName="Section"/>
                <RadioButton Content="Buscar" IsChecked="{Binding IsSearchSelected, Mode=TwoWay}" GroupName="Section"/>
                <RadioButton Content="Crear" IsChecked="{Binding IsCreateSelected, Mode=TwoWay}" GroupName="Section"/>
                <RadioButton Content="Editar" IsChecked="{Binding IsUpdateSelected, Mode=TwoWay}" GroupName="Section"/>
                <RadioButton Content="Dar de baja" IsChecked="{Binding IsDeleteSelected, Mode=TwoWay}" GroupName="Section"/>
            </StackPanel>
        </StackPanel>

        <!-- Content sections -->
        <Grid Grid.Row="1">
            <!-- List section -->
            <StackPanel Visibility="{Binding ListVisibility}" Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <ToggleSwitch Header="Solo activos" IsOn="{Binding OnlyActive, Mode=TwoWay}"/>
                    <Button Content="Cargar empleados" Command="{Binding LoadAllCommand}" Style="{StaticResource JpPrimaryButton}"/>
                </StackPanel>
                
                <Border Style="{StaticResource JpAdminCard}" Height="400">
                    <ListView ItemsSource="{Binding Employees}" SelectedItem="{Binding Selected, Mode=TwoWay}" Style="{StaticResource JpListView}">
                        <ListView.Header>
                            <Grid Padding="8" Background="{StaticResource JpGoldBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="ID" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="Nombre completo" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="Usuario" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="3" Text="Rol" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="4" Text="Activo" Foreground="White" FontWeight="SemiBold"/>
                            </Grid>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8" BorderBrush="{StaticResource JpLineBrush}" BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding IdEmployee}" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding FullName}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Email}" Foreground="{StaticResource JpMutedBrush}" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding UserName}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Role}" VerticalAlignment="Center"/>
                                    <CheckBox Grid.Column="4" IsChecked="{Binding IsActive}" IsEnabled="False" HorizontalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </StackPanel>

            <!-- Search section -->
            <StackPanel Visibility="{Binding SearchVisibility}" Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <TextBox PlaceholderText="Buscar empleados..." Text="{Binding SearchText, Mode=TwoWay}" Width="300"/>
                    <ToggleSwitch Header="Solo activos" IsOn="{Binding OnlyActive, Mode=TwoWay}"/>
                    <Button Content="Buscar" Command="{Binding SearchCommand}" Style="{StaticResource JpPrimaryButton}"/>
                </StackPanel>
                
                <Border Style="{StaticResource JpAdminCard}" Height="400">
                    <ListView ItemsSource="{Binding Employees}" SelectedItem="{Binding Selected, Mode=TwoWay}" Style="{StaticResource JpListView}">
                        <!-- Same template as list section -->
                        <ListView.Header>
                            <Grid Padding="8" Background="{StaticResource JpGoldBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="ID" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="Nombre completo" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="Usuario" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="3" Text="Rol" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="4" Text="Activo" Foreground="White" FontWeight="SemiBold"/>
                            </Grid>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding IdEmployee}" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding FullName}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Email}" Foreground="{StaticResource JpMutedBrush}" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding UserName}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Role}" VerticalAlignment="Center"/>
                                    <CheckBox Grid.Column="4" IsChecked="{Binding IsActive}" IsEnabled="False" HorizontalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </StackPanel>

            <!-- Create section -->
            <StackPanel Visibility="{Binding CreateVisibility}" Spacing="16">
                <TextBlock Text="Crear nuevo empleado y usuario" Style="{StaticResource JpCardTitle}"/>
                
                <Border Style="{StaticResource JpAdminCard}" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Employee info -->
                        <StackPanel Spacing="12">
                            <TextBlock Text="Información del empleado" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBox PlaceholderText="Nombre completo *" Text="{Binding CreateRequest.FullName, Mode=TwoWay}"/>
                            <TextBox PlaceholderText="Email" Text="{Binding CreateRequest.Email, Mode=TwoWay}"/>
                            <TextBox PlaceholderText="Teléfono" Text="{Binding CreateRequest.Phone, Mode=TwoWay}"/>
                            <ComboBox PlaceholderText="Rol" SelectedValue="{Binding CreateRequest.Role, Mode=TwoWay}" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Empleado"/>
                                <ComboBoxItem Content="Admin"/>
                            </ComboBox>
                            <CheckBox Content="Empleado activo" IsChecked="{Binding CreateRequest.IsActive, Mode=TwoWay}"/>
                        </StackPanel>
                        
                        <!-- User info -->
                        <StackPanel Grid.Column="2" Spacing="12">
                            <TextBlock Text="Información de usuario" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBox PlaceholderText="Nombre de usuario *" Text="{Binding CreateRequest.UserName, Mode=TwoWay}"/>
                            <PasswordBox x:Name="CreatePasswordBox" PlaceholderText="Contraseña *" PasswordChanged="CreatePasswordBox_PasswordChanged"/>
                            <TextBox PlaceholderText="Nombre para mostrar" Text="{Binding CreateRequest.DisplayName, Mode=TwoWay}"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button Content="Crear empleado" Command="{Binding CreateCommand}" Style="{StaticResource JpPrimaryButton}" MinWidth="140"/>
                    <Button Content="Limpiar" Command="{Binding ClearCreateCommand}" Style="{StaticResource JpSecondaryButton}" MinWidth="100"/>
                </StackPanel>
            </StackPanel>

            <!-- Update section -->
            <StackPanel Visibility="{Binding UpdateVisibility}" Spacing="16">
                <TextBlock Text="Editar empleado seleccionado" Style="{StaticResource JpCardTitle}"/>
                
                <Border Style="{StaticResource JpAdminCard}" Padding="20">
                    <StackPanel Spacing="12">
                        <TextBox PlaceholderText="Nombre completo" Text="{Binding Editing.FullName, Mode=TwoWay}"/>
                        <TextBox PlaceholderText="Email" Text="{Binding Editing.Email, Mode=TwoWay}"/>
                        <TextBox PlaceholderText="Teléfono" Text="{Binding Editing.Phone, Mode=TwoWay}"/>
                        <ComboBox PlaceholderText="Rol" SelectedValue="{Binding Editing.Role, Mode=TwoWay}" HorizontalAlignment="Stretch">
                            <ComboBoxItem Content="Empleado"/>
                            <ComboBoxItem Content="Admin"/>
                        </ComboBox>
                        <CheckBox Content="Empleado activo" IsChecked="{Binding Editing.IsActive, Mode=TwoWay}"/>
                        
                        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
                            <Button Content="Nuevo" Command="{Binding NewCommand}" Style="{StaticResource JpSecondaryButton}" MinWidth="100"/>
                            <Button Content="Guardar cambios" Command="{Binding SaveCommand}" Style="{StaticResource JpPrimaryButton}" MinWidth="140"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Delete section -->
            <StackPanel Visibility="{Binding DeleteVisibility}" Spacing="16">
                <TextBlock Text="Dar de baja empleado" Style="{StaticResource JpCardTitle}"/>
                
                <Border Style="{StaticResource JpAdminCard}" Padding="20">
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBox PlaceholderText="ID del empleado" Text="{Binding IdLookup, Mode=TwoWay}" Width="200"/>
                            <Button Content="Buscar" Command="{Binding LoadByIdCommand}" Style="{StaticResource JpSecondaryButton}"/>
                        </StackPanel>
                        
                        <StackPanel Visibility="{Binding Selected, Converter={StaticResource NullToVisibilityConverter}}" Spacing="8" Margin="0,12,0,0">
                            <TextBlock Text="{Binding Selected.FullName}" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{Binding Selected.Email}" Foreground="{StaticResource JpMutedBrush}"/>
                            <TextBlock Text="{Binding Selected.Role}" FontWeight="SemiBold"/>
                            
                            <Button Content="Dar de baja" Command="{Binding SoftDeleteCommand}" 
                                    Style="{StaticResource JpPrimaryButton}" 
                                    MinWidth="140" Margin="0,12,0,0"
                                    Background="DarkRed"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Error display -->
            <TextBlock Text="{Binding Error}" Style="{StaticResource JpErrorText}" 
                       VerticalAlignment="Bottom" HorizontalAlignment="Center" 
                       Visibility="{Binding Error, Converter={StaticResource NullToVisibilityConverter}}"
                       Margin="0,0,0,20"/>
        </Grid>
    </Grid>
</UserControl>
