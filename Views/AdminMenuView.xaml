<UserControl
    x:Class="RestaurantJapanese.Views.AdminMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource JpGradientBg}" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título Principal de la Página -->
        <StackPanel Grid.Row="0" Spacing="12" Margin="0,0,0,16" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
                <TextBlock Text="🌸" FontSize="32" VerticalAlignment="Center"/>
                <TextBlock Text="Panel de Administración" Style="{StaticResource JpPageTitle}"/>
                <TextBlock Text="🌸" FontSize="32" VerticalAlignment="Center"/>
            </StackPanel>
            <Rectangle Style="{StaticResource JpSeparator}" Width="250" HorizontalAlignment="Center"/>
            <TextBlock Text="Configura tu restaurante japonés con elegancia y tradición"
                       Style="{StaticResource JpPageSubtitle}"
                       TextAlignment="Center"/>
        </StackPanel>

        <!-- Información del usuario y breadcrumb -->
        <StackPanel Grid.Row="1" Spacing="12" Margin="0,0,0,16">
            <!-- Información del usuario logueado -->
            <Border Background="{StaticResource JpSurfaceBrush}"
                    CornerRadius="{StaticResource JpRadiusM}"
                    Padding="16,12"
                    BorderBrush="{StaticResource JpGoldBrush}"
                    BorderThickness="1"
                    HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="👤" FontSize="18" VerticalAlignment="Center"/>
                        <TextBlock Text="Usuario:" Foreground="{StaticResource JpMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding CurrentUserName}" 
                                   Foreground="{StaticResource JpTextBrush}"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <TextBlock Text="•" Foreground="{StaticResource JpMutedBrush}" VerticalAlignment="Center"/>
                    
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="🏷️" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="Rol:" Foreground="{StaticResource JpMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding CurrentUserRole}" 
                                   Foreground="{StaticResource JpCrimsonBrush}"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Breadcrumb -->
            <Border Background="{StaticResource JpSurfaceBrush}"
                    CornerRadius="{StaticResource JpRadiusM}"
                    Padding="16,10"
                    BorderBrush="{StaticResource JpLineBrush}"
                    BorderThickness="1"
                    HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="&#xE80F;" Style="{StaticResource JpIcon}" FontSize="18"/>
                    <TextBlock Text="Panel de Control" Foreground="{StaticResource JpTextBrush}" FontWeight="SemiBold"/>
                    <TextBlock Text="•" Foreground="{StaticResource JpMutedBrush}"/>
                    <TextBlock Text="Gestión del Sistema" Foreground="{StaticResource JpMutedBrush}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Contenido desplazable -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <!-- Contenedor con ancho máximo para que no "estire" demasiado en pantallas grandes -->
            <Grid MaxWidth="1100" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="320"/>
                    <ColumnDefinition Width="*" MinWidth="320"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- CARD: Empleados -->
                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource JpAdminCard}" Margin="6">
                    <StackPanel Spacing="14">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button Style="{StaticResource JpRoundButton}" 
                                    Content="&#xE716;" 
                                    FontFamily="Segoe MDL2 Assets"
                                    Command="{Binding OpenEmployeesCommand}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Gestión de Empleados" Style="{StaticResource JpCardTitle}"/>
                                <TextBlock Text="Módulo activo"
                                           Foreground="{StaticResource JpBambooBrush}" FontSize="12" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="Administra el personal: altas, bajas, edición y roles (administrador/empleado)."
                                   Style="{StaticResource JpCardDescription}"
                                   TextWrapping="Wrap"/>

                        <Rectangle Height="1" Fill="{StaticResource JpLineBrush}" Margin="0,4"/>

                        <Button Content="Abrir Módulo"
                                Style="{StaticResource JpPrimaryButton}"
                                HorizontalAlignment="Stretch"
                                Command="{Binding OpenEmployeesCommand}"/>
                    </StackPanel>
                </Border>

                <!-- CARD: POS -->
                <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource JpAdminCard}" Margin="6">
                    <StackPanel Spacing="14">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button Style="{StaticResource JpRoundButtonOrange}" 
                                    Content="&#xE7BF;" 
                                    FontFamily="Segoe MDL2 Assets"
                                    Command="{Binding OpenPosCommand}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Punto de Venta" Style="{StaticResource JpCardTitle}"/>
                                <TextBlock Text="Módulo activo"
                                           Foreground="{StaticResource JpBambooBrush}" FontSize="12" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="Vende directo desde el POS: carrito, IVA, propina y emisión de tickets."
                                   Style="{StaticResource JpCardDescription}"
                                   TextWrapping="Wrap"/>

                        <Rectangle Height="1" Fill="{StaticResource JpLineBrush}" Margin="0,4"/>

                        <Button Content="Abrir POS"
                                Style="{StaticResource JpAccentButton}"
                                HorizontalAlignment="Stretch"
                                Command="{Binding OpenPosCommand}"/>
                    </StackPanel>
                </Border>

                <!-- CARD: Inventario (MENÚ) — ACTIVO -->
                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource JpAdminCard}" Margin="6">
                    <StackPanel Spacing="14">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button Style="{StaticResource JpRoundButtonOrange}" 
                                    Content="&#xE7C7;" 
                                    FontFamily="Segoe MDL2 Assets"
                                    Command="{Binding OpenInventoryCommand}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Gestión de Productos (Inventario)" Style="{StaticResource JpCardTitle}"/>
                                <TextBlock Text="Módulo Activo" Foreground="{StaticResource JpBambooBrush}"
                                           FontSize="12" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="Administra el menú: platillos, precios y disponibilidad."
                                   Style="{StaticResource JpCardDescription}" TextWrapping="Wrap"/>

                        <Rectangle Height="1" Fill="{StaticResource JpLineBrush}" Margin="0,4"/>

                        <Button Content="Abrir Inventario"
                                Style="{StaticResource JpPrimaryButton}"
                                HorizontalAlignment="Stretch"
                                Command="{Binding OpenInventoryCommand}"/>
                    </StackPanel>
                </Border>

                <!-- Análisis y Reportes (ACTIVO) -->
                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource JpAdminCard}" Margin="6">
                    <StackPanel Spacing="14">
                        <!-- Header con ícono -->
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button Style="{StaticResource JpRoundButtonGreen}" 
                                    Content="&#xE9D2;" 
                                    FontFamily="Segoe MDL2 Assets"
                                    Command="{Binding OpenSalesReportCommand}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Análisis y Reportes"
                                           Style="{StaticResource JpCardTitle}"/>
                                <TextBlock Text="Ventas por día, semana y mes"
                                           Foreground="{StaticResource JpBambooBrush}"
                                           FontSize="12"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="Consulta gráficos y tablas de ventas. Filtra por día/semana/mes y exporta."
                                   Style="{StaticResource JpCardDescription}"/>

                        <Rectangle Height="1" Fill="{StaticResource JpLineBrush}" Margin="0,4"/>

                        <!-- BOTÓN ACTIVO -->
                        <Button Style="{StaticResource JpSecondaryButton}"
                                Content="Abrir Reportes"
                                Command="{Binding OpenSalesReportCommand}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

    </Grid>
</UserControl>
